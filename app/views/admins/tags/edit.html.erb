<div class="row">
  <%= breadcrumb do %>
    <%= breadcrumb_item %>
    <%= breadcrumb_item 'Tags', admins_tags_path %>
    <%= breadcrumb_item 'Edit Tag' %>
  <% end %>
</div>

<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header">Edit <%= @tag.name.titleize %> Tag</h1>
  </div>
</div>

<%= render 'form', tag: @tag %>
