<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-body">
        <%= form_for([:admins, newsletter_queue], :html => { :class => 'form-horizontal' }) do |f| %>
          <% if newsletter_queue.errors.any? %>
            <div class="alert alert-danger" role="alert">
              <strong>Ops! <%= pluralize(newsletter_queue.errors.count, "error") %> found:</strong><br/>
              <ul>
                <% newsletter_queue.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <div class="form-group">
            <%= f.label :subject, :class => 'col-sm-2 control-label' %>
            <div class="col-sm-10">
              <%= f.text_field :subject, :class => 'form-control' %>
            </div>
          </div>

          <div class="form-group">
            <%= f.label :content, :class => 'col-sm-2 control-label' %>
            <div class="col-sm-10">
              <%= f.cktext_area :content, :class => 'ckeditor' %>
            </div>
          </div>

          <div class="row">
            <div class="col-md-6 text-right">
              <%= link_to admins_newsletter_queues_path, :class => 'btn btn-warning' do %>
                <span class="glyphicon glyphicon-menu-left"></span>&nbsp;Back
              <% end %>
            </div>
            <div class="col-md-6 text-left">
              <%= f.submit 'Save', :class => 'btn btn-primary' %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
